import 'package:flutter/material.dart';
class per_wallet extends StatefulWidget {
  const per_wallet({Key key}) : super(key: key);

  @override
  _per_walletState createState() => _per_walletState();
}

class _per_walletState extends State<per_wallet> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Personel wallet'),
    );
  }
}
