import 'package:flutter/material.dart';

class dash_settings extends StatefulWidget {
  const dash_settings({Key key}) : super(key: key);

  @override
  _dash_settingsState createState() => _dash_settingsState();
}

class _dash_settingsState extends State<dash_settings> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Dashboard settings'),
    );
  }
}
